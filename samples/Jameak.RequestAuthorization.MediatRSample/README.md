## MediatR sample

This is ASP.NET Core sample application that uses MediatR for requests/response handling.

It registers requirement builders and requirement handlers and a global requirement builder via assembly scanning.