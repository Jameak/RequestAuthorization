## Mediator.SourceGenerator sample

This is a NativeAOT ASP.NET Core sample application that uses Mediator.SourceGenerator for requests/response handling.

It registers a requirement builder and a requirement handler explicitly, and configures Mediator.SourceGenerator for NativeAOT using the pipeline configuration on `MediatorOptions`.